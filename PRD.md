# 产品需求文档（PRD）：今日热搜

> **文档版本**：v1.2（MVP）
> **撰写日期**：2026-09-21（Day 4）；v1.2 为同日追加修订
> **适用项目**：今日热搜网站
> **上游文档**：`research.md`（Day 3 需求研究）
> **文档性质**：本文档规定「做成什么样才算完成」，不涉及技术选型、不涉及代码
> **自检状态**：✅ 已通过 AI 自检，🔴 必须修改级问题 **0 项**（自检发现的问题与修订明细见文末「附录：自检与变更记录」）
> **本期范围**：本产品的长期形态是「两个模式」（见 1.6），但**本期只实现模式一**。v1.1 → v1.2 的修订**未改动本期 MVP 范围**

---

## 1. 项目概述

### 1.1 一句话定义

> **今日热搜**是一个打开就能看的网页，帮用户在 3 分钟内知道今天全网在传什么。

### 1.2 这个产品要解决什么问题

用户想看今天的热点，现在得打开微博刷一遍、知乎刷一遍、抖音刷一遍，加起来 20 分钟，还容易刷着刷着跑偏。

**我们要解决的不是"信息不够"，而是"信息太多、不知道看哪个"。**

### 1.3 这个产品不解决什么问题

- 不解决"我想看某个平台的完整榜单"（那该去用 `tophub.today`）
- 不解决"我要追踪某个话题的长期走势"（那该去用 `TrendRadar`）—— **不过这一项已列入 5.4 的未来路线图**，属于本期不做、将来要做
- 不解决"我要做内容运营/舆情分析"（专业工具更合适）

### 1.4 本文档管什么、不管什么

| | 内容 |
|---|---|
| **本文档管** | 有几个页面、每页显示什么、用户能做什么操作、怎么做算做完 |
| **本文档不管** | 用什么技术实现（Day 5 技术选型）、代码怎么写（Day 7 起）、怎么部署上线 |

### 1.5 与上游文档的对应关系

| `research.md` 中的结论 | 在本文档中的落点 |
|---|---|
| 目标用户 = 想快速了解热点的普通网民 | → 第 2 节 |
| 定位 = 热度速览 + 关键词框 | → 第 1.1 节、第 4 节 |
| MVP 4 项功能 | → 第 4 节、第 7 节 |
| 本期不做清单 | → 第 5 节 |
| 改变想法的地方（从聚合改为过滤） | → 第 4.3 节 |
| 未来想做的功能（推送通知、AI 分析） | → 第 5.4 节（已扩充为 3 项） |

### 1.6 产品蓝图：两个模式

本产品的长期形态是**两个并列的模式**，共用同一个入口。

| 模式 | 解决什么问题 | 用户输入 | 用户得到 | 本期状态 |
|---|---|---|---|---|
| **模式一：热点速览** | 我不知道今天该看什么 | 关键词（可不填） | 按热度排序的热点列表 | ✅ **本期实现** |
| **模式二：文件速查** | 某项政策 / 文件的原文是怎么说的 | 关键词 | 官方文件中的相关原文段落 + AI 辅助说明 | ⏳ 未来实现（见 5.4） |

**为什么用「模式」区分，而不是做成两个产品**：这两件事的用户其实是**同一批人** —— 关心 AI 的人，既想知道「AI 今天上热搜了」，也想查「政策文件里到底怎么写的」。一个入口解决两件事，用户不用记两个网址。

**为什么本期只做模式一**：模式二需要官方文件来源、后端服务、AI 接口，三样现在都没有（详见 5.4）。**先把模式一做扎实，再开模式二** —— 两个模式同时开工，结果往往是两个都做不深。

**模式是「设计」，不是「本期功能」**：1.6 与 5.4 都属设计层面的规划，**不产生本期要写的代码**。这不是拖延 —— 它让将来真正动手时，不必把这些问题重想一遍。

---

## 2. 用户与场景

### 2.1 目标用户

**想快速了解全网热点、但不想在多个 App 之间来回切的普通网民。**

| 维度 | 描述 |
|---|---|
| 身份 | 大学生、上班族为主 |
| 技术水平 | 普通人。不会为了看热点去学配置、装软件 |
| 核心诉求 | **用最短的时间**知道今天发生了什么 |
| 设备 | 桌面浏览器（本期只要求桌面端可用，见 5.3） |

### 2.2 使用场景（3 个具体情境）

1. **早上刚到教室 / 工位**：打开电脑浏览器，往下滑两下，3 分钟内知道今天在传什么
2. **午休刷一下**：只想看自己关心的领域，输入「考研」或「AI」，只看这几条
3. **睡前扫尾**：白天没空，晚上一次性补上今天的热点

三个场景的共同点：**用户是"路过"，不是"坐下"**。他不会在页面上停留很久。

### 2.3 非目标用户（明确排除）

| 这类人 | 为什么不是我们的用户 |
|---|---|
| 自媒体运营 | 需要完整榜单和批量导出，我们的 30 条不够 |
| 舆情分析人员 | 需要历史数据、数据导出、趋势图，我们一项都没有 |
| 想"逛"内容的人 | 需要不断加载新内容，我们是一个固定列表 |

---

## 3. 页面清单

**本期只做 1 个页面。**

| # | 页面 | 作用 | 进入方式 |
|---|---|---|---|
| 1 | **首页（唯一页面）** | 展示热点列表 + 提供关键词筛选 | 打开网站即进入 |

### 3.1 为什么只有一个页面

速览、输入框、结果列表、空状态提示**全部在这一个页面内完成**，不需要任何页面跳转。

- 多一个页面 = 多一套"怎么跳过去、怎么跳回来"的逻辑，MVP 阶段不值得
- 用户的核心动作是"扫一眼"，跳来跳去反而打断
- 后续如果要加"热点详情页"，放到 Day 28 之后再考虑

### 3.2 本期不做第二个页面

明确不做的页面（若以后要做，需重新评审）：

- 热点详情页
- 关于/说明页
- 任何"设置"页面

---

## 4. 页面细节：首页

### 4.1 页面结构（从上到下）

```
┌──────────────────────────────────────────────┐
│  ① 顶部区                                     │
│     网站名「今日热搜」                          │
│     一句话说明：3 分钟看完今天全网在传什么        │
├──────────────────────────────────────────────┤
│  ② 筛选区                                     │
│     ┌──────────────────────────────────┐    │
│     │ 🔍 输入关键词，只看你关心的          │    │
│     └──────────────────────────────────┘    │
├──────────────────────────────────────────────┤
│  ③ 列表区                                     │
│     01  某新能源汽车品牌发布新款                │
│         微博 · 热度 874498                    │
│     ─────────────────────────────────        │
│     02  另一个热点标题                         │
│         知乎 · 热度 19000                     │
│     ─────────────────────────────────        │
│     ……（共 30 条）                            │
├──────────────────────────────────────────────┤
│  ④ 空状态（仅当筛选无结果时，替换整个列表区）     │
│     没有找到相关内容                            │
└──────────────────────────────────────────────┘
```

页面由 **4 个区块**组成：顶部区、筛选区、列表区、空状态提示。没有侧边栏、没有底部导航。

### 4.2 条目字段定义

每条热点在页面上显示 **4 项信息**：排名、标题、来源平台、热度值。四项缺一不可。

但**只有 3 项来自数据文件**，排名是算出来的 —— 这个区分直接决定 Day 7 的数据结构怎么写，所以必须说清：

| 显示项 | 含义 | 类型 | 示例 | 来自哪里 | 说明 |
|---|---|---|---|---|---|
| **排名** | 该条目在**当前列表**中的位置 | 整数 | `1` | **前端实时计算** | 从 1 开始连续编号。**不存进数据文件** —— 因为筛选之后要重新编号（见 4.3 交互 5），数据里存了它反而会和计算结果打架 |
| **标题** | 热点内容标题 | 文本 | 某新能源汽车品牌发布新款 | 数据文件 | 关键词筛选**只**比对这个字段 |
| **来源平台** | 这条热点来自哪个平台 | 文本 | 微博 / 知乎 / 百度 | 数据文件 | 用于让用户知道出处。**不得出现在标题里**（见 4.4） |
| **热度值** | 该条目的热度数值，**同时充当排序依据** | 整数 | `874498` | 数据文件 | 性质说明见 4.4，**不要误当成"跨平台客观热度"** |

> **一句话记法**：数据文件里每条存 **3 样**（标题、来源平台、热度值），页面上显示 **4 样**（多一个排名）。

### 4.3 交互定义

| # | 用户动作 | 系统响应 | 对应原规划功能 |
|---|---|---|---|
| 1 | 打开页面 | 直接显示 30 条热点，**无需任何点击** | 原规划第 1 项「全网 Top 30 速览」（"全网"二字已按 4.4 收窄） |
| 2 | 在输入框输入关键词 | **实时**筛选列表，只保留标题包含该关键词的条目 | 单关键词过滤 |
| 3 | 清空输入框 | 恢复显示全部 30 条，顺序与初始一致 | 单关键词过滤 |
| 4 | 点击任意条目 | **新窗口**打开搜索引擎结果页，搜索词 = 该条目标题 | 点击跳转原文（**已调整，见 5.1**） |
| 5 | 输入无匹配的关键词 | 列表区替换为「没有找到相关内容」 | 空状态提示 |
| 6 | 输入框里**只有空格** | 视为"空"，恢复显示全部 30 条，**不触发空状态** | 边界处理 |

**交互细节要求：**

- 筛选是**即时的**：输入框内容一变，列表立刻跟着变。**不做"搜索"按钮，不需要按回车**
- 筛选**不区分大小写**：输入 `ai` 和 `AI` 结果一致
- 筛选**只匹配标题**：不匹配来源平台、不匹配热度值
- 匹配前先**去掉关键词首尾空格**：输入 `" AI "` 与 `"AI"` 结果一致
- 排名在筛选后**重新编号**：筛出 5 条时，显示 01~05
- 点击条目时**当前页面不跳转**：新窗口承载搜索结果，原列表保持原样
- 搜索引擎**固定用百度**（`https://www.baidu.com/s?wd=<搜索词>`）。理由：国内可直接访问，不需要额外条件
- 拼网址前必须对标题做 **URL 编码**：标题里可能出现 `&`、`#`、`?`、空格、中文等字符，不编码会导致跳转链接断裂或搜索词被截断（例如标题含 `&` 时不编码，`&` 后面的内容会被丢掉）

### 4.4 数据说明（重要）

**本期使用 30 条手写示例数据**，不接真实数据源。

原因：真实数据需要抓取，涉及法律风险（详见 5.2）。

示例数据的字段结构（标题、来源平台、热度值）与真实数据**完全一致**，所以将来换成真实数据时，页面逻辑不需要改。排名则由页面按 4.2 的规则实时算出，与数据源无关。

**示例数据的准备要求（Day 7 开发时必须满足）：**

| 要求 | 说明 |
|---|---|
| 条数 | 正好 **30 条** |
| **标题里不得出现平台名** | 30 条标题中**不得出现**「微博」「知乎」「百度」这类平台名。原因：验收标准 B2 要用一个"只出现在来源平台字段里、不出现在任何标题里"的词，来验证"筛选只匹配标题"这条规则。如果标题里也有平台名，B2 就没法验证了 |
| 热度值互不相同 | 30 条的数值两两不等，避免出现"两条并列第几"的排序歧义 |

**关于「热度值」的一个重要说明：**

**本期（示例数据阶段）的做法：**

| 事项 | 说明 |
|---|---|
| **排序依据** | 按热度值**从高到低**排列，排名从 1 连续编号到 30 |
| **热度值是什么** | 它是**排序权重** —— 决定这 30 条谁排前面、谁排后面。示例数据中该数值由人工设定 |
| **不写「全网热度」** | 示例数据不可能是真实的全网统计，文档和页面上都不做这个宣称 |
| **不可跨平台比较** | 微博用数字（如 874498）、知乎用"1.9万热度"、百度叫"热搜指数"，**三家的口径完全不同**。所以这个数值的作用是"排序"，**不是**"证明微博这条比知乎那条更热" |

> **为什么要把这一条掰得这么细**：本文档 v1.0 里同时写了「按热度值从高到低排列」和「热度值不可跨平台比较」两句 —— 这两句放在一起是**自相矛盾**的：既然数值不可比，那拿它排序就不成立。v1.1 的解法是把「排序依据」和「客观热度结论」分开：**排序规则可验证（数值递减），数值的性质如实说明（仅作排序权重）**。这是本次自检发现的第一个 🔴 问题。

**长期演进的方向：把「热度」换成「频次」**

> 上面「不可跨平台比较」那一条，是**平台给的数值**天生的毛病 —— 每家算法都不公开，没法放在一起比。
>
> 长期的解法是**换一个可比的度量：跨平台出现频次** —— 不问「你有多热」（那是平台说了算），改问「**你在几个平台、被提到了几次**」（这个自己就能数）。
>
> 这个转变的意义在于：**把不可比的东西换成了可比的东西**。所以本文档现在宁可把热度值如实写成「排序权重」，也不写成「客观热度」—— 那一层含义最终要交给「频次」来承担。
>
> 具体形态见 5.4 第 1 项。**本期不实现**（需要真实的多平台数据）。

---

## 5. 边界：不做什么

### 5.1 本次砍掉的功能（含理由）

> **这一节回答的问题**：从 `research.md` 到 PRD，写具体规格的过程中砍掉了什么？为什么？

#### 砍掉一：跳转到原平台原文

| | |
|---|---|
| **原规划** | （`research.md` MVP 第 3 项）点击条目，跳转到原平台查看原文 |
| **砍掉的原因** | 本期用的是**示例数据**，那些"原文链接"跳过去全是**死链**。**一个点了没反应的网站，比一个不能点的网站更糟** —— 用户会以为网站坏了，而不是以为数据是假的 |
| **替代方案** | 点击条目 → 新窗口打开**搜索引擎结果页**，以该条目标题为搜索词 |
| **为什么选这个替代方案** | ① 技术成本极低（拼接一个网址即可）② 不涉及爬虫、不涉及侵权 ③ 结果真的对用户有用 —— 他确实能找到这条热点 |
| **这个替代方案的代价** | 严谨地说，「跳到原平台看原文」这个能力**确实没有实现**。用户看到的是搜索结果，不是原帖 |

#### 砍掉二：条目摘要

| | |
|---|---|
| **原考虑** | 参考 `rebang.today` 的做法，每条热点下方带一段摘要文字 |
| **砍掉的原因** | 本期是手写示例数据，加摘要就意味着要为 30 条**各编一段假的摘要文本**。工作量翻倍，而**前端技能的学习收获为零** —— 摘要只是一个多出来的文本字段 |
| **代价** | 用户光看标题可能不知道具体发生了什么。但我们的定位是"扫一眼"，标题足够完成这个任务 |

#### 收窄三：不宣称「全网热度」

| | |
|---|---|
| **原表述** | `research.md` 中写的是「全网热度 Top 30」 |
| **收窄后的表述** | 「按预设热度值从高到低排列的 30 条热点」 |
| **后续安排** | 换成真实数据后，「热度」**不再沿用各平台的原始数值**，而是改用**跨平台出现频次**重新计算（见 5.4 第 1 项）。到那时这句话可以正式恢复为「全网热度」—— 因为频次是**自己算的、口径统一的**，说这话就不再是虚假承诺 |
| **原因** | 用示例数据时，所谓"热度"其实是预先排好的顺序，不是真实的全网统计。**不能对用户做出做不到的承诺** |

### 5.2 明确不做的功能清单（沿用 `research.md`，收窄到本期代码范围）

| 不做的事 | 原因 |
|---|---|
| 多个关键词 / AND OR 逻辑 | 复杂度急剧上升，不是 28 天能完成的 |
| 屏蔽词（如 `!明星`） | 属于进阶语法，本期用不到 |
| 同义词扩展（输入"AI"也匹配"人工智能"） | 需要词库 |
| 保存关键词、记住用户偏好 | 需要本地存储 / 账号体系 |
| 平台筛选 Tab | 有余力再说，不是命脉功能 |
| 用户账号 / 收藏 / 评论 / 分享 | 社交功能，一个都不做 |
| 历史趋势 / 周榜月榜 | 需要长期存数据。**已列入 5.4 第 2 项的未来路线图**（本期不做） |
| 推送通知 / AI 舆情分析 | 前置条件不具备，记入 roadmap（见 5.4 与 `research.md` §8） |
| 自己写爬虫抓真实数据 | **法律风险 + 技术风险，明确不做** |
| 自定热度权重算法 | 属于工程化产物，不是现阶段该碰的 |

### 5.3 本文档不负责的事

| 事项 | 由谁负责 |
|---|---|
| 用什么框架 / 目录结构怎么组织 | Day 5 技术选型 |
| 代码怎么写、组件怎么拆 | Day 7 起开发阶段 |
| 怎么部署、部署到哪里 | 课程后期 |
| 移动端适配 | **本期不做**，只要求桌面浏览器可用 |
| 视觉设计细节（配色、字体、动效） | 开发阶段边做边定 |

### 5.4 未来想做（本期不做，但已列入路线图）

> **与 5.1 / 5.2 的区别**
> - **5.1 / 5.2** 是**砍掉、不做**的东西 —— 其中有些是永远不做（如自写爬虫）
> - **本节**是**明确要做、但现在做不了**的东西 —— 记下来是为了不忘，也是为了写清「等到什么条件才能开始」
>
> 本节扩充了 `research.md` §8、§9 的路线图。

以下 3 项按**前置条件的满足顺序**排列（不是按重要性排列）。三项都不能在 28 天内开始，其中第 3 项同时也是「模式二」的主体。

#### 第 1 项：跨平台出现频次（「热度」的升级做法）

| | |
|---|---|
| **价值** | 从根上解决「平台给的数值不可比」。用「被提到几次」代替「有多热」，口径统一、可自行计算 |
| **将来怎么用** | 用户输入「AI」→ 页面显示：总计出现 **23** 次、覆盖 **6** 个平台，下面按平台列出分布（微博 8 次、知乎 5 次、百度 4 次……） |
| **前置条件** | ① 真实的多平台数据源（本节三项共用这个前提）② 数据里带「平台」维度 ③ 能持续获取，不是抓一次就完 |
| **什么时候做** | 28 天课程结束后，第一批 |
| **待定** | 数的是「关键词出现次数」还是「同一事件的报道数」？见第 6 节待定问题第 8 条 |

#### 第 2 项：关键词热度趋势曲线

| | |
|---|---|
| **价值** | 看出一件事**正在升温还是已经凉了**。只看今天的热度值，是看不出趋势的 |
| **将来怎么用** | 用户先定几个关注的关键词（如「AI」「考研」）→ 用折线图展示这些词**近 7 天**的热度变化 |
| **前置条件** | ① 第 1 项先做出来 ② **每天的数据快照要存下来**（不能算完就丢）③ 后端定时抓取 ④ 数据库 |
| **什么时候做** | 数据库阶段（Day 23–26）之后，且在第 1 项之后 |

#### 第 3 项：模式二「官方文件速查」

| | |
|---|---|
| **价值** | 会议 / 政策文件发布后，帮用户**直接找到与自己关心的事相关的那几段原文**，而不是通读全文 |
| **将来怎么用** | 用户切到「文件速查」模式 → 输入关键词 → 页面列出**命中该词的官方文件**及其**原文段落** → AI 补充说明背景与术语 |
| **前置条件** | ① 官方文件的来源（且获取方式要合规）② 后端服务 + AI 接口（**AI 的 Key 绝不能放在前端**）③ 全文检索能力 |
| **什么时候做** | 模式一稳定之后 |

**这里有一条实现顺序，将来不要颠倒：**

| 步骤 | 做什么 | 为什么先做这个 |
|---|---|---|
| **第一步** | **只做「定位」**：关键词全文检索，把原文段落找出来、标出命中位置 | 检索**准确、可验证、不花钱**。用户看到的是「原文里确实有这句」—— 可信 |
| **第二步** | **再加「辅助解读」**：AI 解释术语、补充背景 | AI 是**增值**，但会出错。所以必须建立在「原文已经摆出来」的基础上，让用户随时能对照原文判断 |

**如果顺序反了**（先上 AI 解读、却不展示原文），用户看到的是「AI 说的话」，不是「文件里写的字」—— 可信度和责任都会出问题。

**两条不可动摇的原则：**

1. **以原文为准**：AI 输出的内容必须标注「AI 生成，仅供参考，请以官方原文为准」，并附原文出处
2. **只做辅助，不代替判断**：网站**不输出自己的立场或结论**，只帮用户更快找到原文、看懂原文

---

## 6. 待定问题

以下问题现在定不了，先记下来，并注明什么时候定。

| # | 问题 | 什么时候定 |
|---|---|---|
| 1 | 用哪个技术栈 / 框架 | **Day 5**（技术选型） |
| 2 | 示例数据放在哪里、用什么格式存放 | Day 7 开发时 |
| 3 | 热度值要不要格式化成「万」（如 1.9万） | Day 7 开发时看实际效果。**注意**：格式化只影响"显示"，**排序必须用原始数值** —— 否则 `1.9万` 会排在 `874498` 前面（按字符串比大小），排序就错了 |
| 4 | 页面宽度、配色方案 | Day 7 开发时 |
| 5 | 网站名「今日热搜」是否要改 | 部署上线前 |
| 6 | 真实数据源怎么接 | **28 天课程结束后** |
| 7 | 是否要加移动端适配 | Day 28 之后评估 |
| 8 | 「频次」数的是**关键词出现次数**，还是**同一事件的报道数**（后者要先把「同一件事」归并起来，是另一个量级的难度） | 做 5.4 第 1 项之前 |
| 9 | 模式二的官方文件从哪里获取、以什么方式获取 | 做 5.4 第 3 项之前 |
| 10 | 两个模式之间怎么切换（顶部 Tab / 首页入口 / 侧边栏） | 开发模式二之前 |

---

## 7. 验收标准

> **使用说明**：本节是整份文档的核心。以下每一条都应当**只能判"通过"或"不通过"**，不出现"基本可以""差不多"。
> Day 27 收尾自检时，逐条打勾使用。

### A. 速览（对应 `research.md` MVP 第 1 项）

| # | 验收标准 | 通过条件 |
|---|---|---|
| **A1** | 打开页面即显示内容 | 打开页面后，**不点击任何按钮**，能直接看到 **30 条**热点条目 |
| **A2** | 排序正确 | 列表里**上下相邻的任意两条**，上一条的热度值 **≥** 下一条（即整体按热度值从高到低）；排名从 1 **连续**编号到 30，无重复、无跳号、无并列 |
| **A3** | 信息完整 | **每一条**都同时显示 **4 项信息**：排名、标题、来源平台、热度值。任意一条缺任意一项即不通过 |

### B. 关键词筛选（对应 `research.md` MVP 第 2 项）

| # | 验收标准 | 通过条件 |
|---|---|---|
| **B1** | 实时筛选生效 | 在输入框输入「AI」，列表**立即**只剩标题包含「AI」的条目。**无需点按钮、无需按回车** |
| **B2** | 只匹配标题 | 输入「**微博**」（该词只出现在部分条目的"来源平台"字段里，并按 4.4 的数据准备要求**不出现在任何标题里**），列表应为**空**并触发空状态。只要出现任何一条，就说明它匹配到了来源平台字段 → **不通过** |
| **B3** | 清空可复原 | 清空输入框后，列表**恢复显示全部 30 条**，顺序与初始状态完全一致 |
| **B4** | 不区分大小写 | 输入 `ai` 与输入 `AI`，筛选结果**完全一致** |
| **B5** | 筛选后重新编号 | 筛出 5 条时，排名显示为 01~05，**不是**原来的 03、07、12… |

### C. 点击条目（对应 `research.md` MVP 第 3 项 —— 已按 5.1 调整）

| # | 验收标准 | 通过条件 |
|---|---|---|
| **C1** | 点击打开新窗口 | 点击任意条目，**新窗口**打开百度搜索结果页（网址以 `baidu.com/s?wd=` 开头），搜索框里显示的词等于该条目的**完整标题** —— 含 `&`、空格等符号时也没被截断 |
| **C2** | 原页面不跳转 | 点击后当前页面**不跳转、不刷新**，原列表仍在原位置 |
| **C3** | 整条可点 | 点击条目的任意位置（标题、平台名、热度值）都能触发，不是只有标题可点 |

### D. 空状态（对应 `research.md` MVP 第 4 项）

| # | 验收标准 | 通过条件 |
|---|---|---|
| **D1** | 无结果有提示 | 输入无匹配的词（如 `zzz`），列表区显示「**没有找到相关内容**」 |
| **D2** | 不残留旧内容 | 空状态下**不出现**空白区域、不出现筛选前的旧条目 |
| **D3** | 可从空状态恢复 | 清空输入框，能正常恢复为 30 条列表，页面不卡死、不报错 |

### E. 整体可用性

| # | 验收标准 | 通过条件 |
|---|---|---|
| **E1** | 桌面浏览器可用 | 在桌面浏览器打开，页面正常显示，浏览器控制台**无报错** |
| **E2** | 刷新后状态正常 | 刷新页面后回到初始状态（30 条全量），不报错、不出现空白页 |
| **E3** | 数据是示例数据 | 页面上**必须有一处可见的文字**（页头或页脚一行即可）说明当前展示的是示例数据。**不能只是"口头约定"** —— 截图交作业、给别人看时，必须一眼能看出来 |

### F. 本期不纳入验收的项

以下**不在**本期验收范围内，不通过也不影响 MVP 完成：

- 移动端 / 小屏显示效果
- 加载动画、过渡效果
- 视觉美观度的主观评价
- 实际性能指标（30 条数据的加载速度）
- 真实数据的准确性

---

## 8. 下一步

| 时间 | 做什么 |
|---|---|
| **Day 5** | 技术选型 —— 确定用什么技术实现本文档第 4 节的要求 |
| Day 7 起 | 按第 4 节「页面细节」开始写代码 |
| Day 27 前后 | 用第 7 节「验收标准」逐条打勾自检 |

**本文档不含技术选型，不含代码，符合 Day 4 的任务边界。**

---

## 9. 验收记录（Day 27 自检时填写）

**现在留空。** 开发完成后逐条填「通过 / 不通过」。

规矩说在前面：**任何一条不通过，都要回去改代码，不准改标准。** 改标准 = 自己给自己放水，那这份文档就白写了。

| 编号 | 标准摘要 | 结果 | 备注 |
|---|---|---|---|
| A1 | 打开即见 30 条 | ☐ 通过 ☐ 不通过 | |
| A2 | 按热度值降序、排名 1~30 连续 | ☐ 通过 ☐ 不通过 | |
| A3 | 每条 4 项信息齐全 | ☐ 通过 ☐ 不通过 | |
| B1 | 输入「AI」实时筛选 | ☐ 通过 ☐ 不通过 | |
| B2 | 输入「微博」结果为空 | ☐ 通过 ☐ 不通过 | |
| B3 | 清空后恢复 30 条 | ☐ 通过 ☐ 不通过 | |
| B4 | `ai` 与 `AI` 结果一致 | ☐ 通过 ☐ 不通过 | |
| B5 | 筛选后排名重新编号 | ☐ 通过 ☐ 不通过 | |
| C1 | 新窗口打开百度搜索该标题 | ☐ 通过 ☐ 不通过 | |
| C2 | 原页面不跳转、不刷新 | ☐ 通过 ☐ 不通过 | |
| C3 | 整条任意位置可点 | ☐ 通过 ☐ 不通过 | |
| D1 | 无结果显示提示文字 | ☐ 通过 ☐ 不通过 | |
| D2 | 空状态不残留旧条目 | ☐ 通过 ☐ 不通过 | |
| D3 | 从空状态能恢复 | ☐ 通过 ☐ 不通过 | |
| E1 | 桌面浏览器打开、控制台无报错 | ☐ 通过 ☐ 不通过 | |
| E2 | 刷新后回到初始状态 | ☐ 通过 ☐ 不通过 | |
| E3 | 页面有"示例数据"说明文字 | ☐ 通过 ☐ 不通过 | |

---

## 10. 用户故事（余力加练）

> Day 4 的余力加练题是「让 AI 把一项功能写成用户故事」。这里挑 **MVP 第 2 项「单关键词过滤」** 来写 —— 它是本期唯一需要用户动手操作的功能。

### 10.1 主故事

**作为一个**只想看自己关心领域的普通用户，
**我希望**在页面输入框里敲一个词、列表立刻只剩跟这个词有关的热点，
**以便**我不用在几十条无关内容里翻找，3 分钟里看到的东西全是我关心的。

### 10.2 这个故事里的三个要素

| 要素 | 在这个故事里是谁 / 是什么 |
|---|---|
| **角色** | 想快速了解热点的普通网民（见第 2.1 节），不是运营、不是分析人员 |
| **目标** | 在一堆信息里，快速捞出自己关心的那几条 |
| **价值** | 省时间 —— 这正是本产品唯一存在的理由 |

### 10.3 验收条件（与第 7 节 B 组一一对应）

| # | 条件 | 对应 |
|---|---|---|
| 1 | 输入「AI」，列表立刻只剩标题含「AI」的条目 | B1 |
| 2 | 输入 `ai` 的结果与 `AI` 完全一致 | B4 |
| 3 | 清空输入框，30 条全部回来 | B3 |
| 4 | 输入没有匹配的词，显示「没有找到相关内容」，不是一片空白 | D1 |
| 5 | 输入只出现在"来源平台"里的词（如「微博」），列表应为**空** | B2 |

### 10.4 这个故事里**不**包含什么

- **不支持多个词**：一个词，够用了
- **不记住上次搜的词**：刷新即重置
- **不做"搜索"按钮**：边打字边出结果
- **不猜同义词**：输入「AI」不会自动匹配「人工智能」

> **用户故事到底有什么用**：它逼着人把"我要做什么功能"换成"用户要达成什么目的"。
> 写上面这段的时候，第 5 条验收条件（输入「微博」应该搜不到）就是被故事逼出来的 —— 只盯着功能想，根本不会想到这一层。**这也是「只匹配标题」这条规则在 PRD 里能站住脚的原因。**

---

## 附录：自检与变更记录

### v1.2（2026-09-21，Day 4 当日追加）

按子豪在板块③之后提出的新想法修订。**本次修订不改变本期 MVP 范围**，只做两件事：把「热度」的长期形态写清楚，以及新增一节路线图。

#### 新增

| 节 | 内容 |
|---|---|
| **1.6** | 产品蓝图：两个模式（热点速览 / 文件速查），明确本期只做模式一，并说明为什么用「模式」而不是「两个产品」 |
| **5.4** | 未来想做（路线图）：跨平台频次、热度趋势曲线、官方文件速查，各含价值 / 将来怎么用 / 前置条件 / 什么时候做 |

#### 修订

| 节 | 修订 |
|---|---|
| **4.4** | 「热度值」增加一段「长期演进的方向」：从「平台给的数值排序」演进为「**跨平台出现频次**」。理由：平台数值口径不可比（各家算法不公开），而频次是**可自行计算、口径统一**的 |
| **5.1 收窄三** | 补充「后续安排」：换成真实数据后热度改用频次重算，届时可正式恢复「全网热度」的说法 |
| **1.5** | 与上游文档的对应关系表，补入「未来想做的功能 → 5.4」一行 |
| **6** | 待定问题新增 3 条（频次口径、官方文件来源、模式切换方式） |

#### 为什么这次修订不动本期 MVP

新提的三件事，每一件都卡在同一个地方：**需要真实数据**。

| 想法 | 卡在哪一步 |
|---|---|
| 跨平台频次 | 30 条手写示例数据统计出来的「频次」是编的，毫无意义 |
| 热度趋势曲线 | 需要连续 7 天的数据快照 —— 现在没有任何历史数据 |
| 官方文件速查 | 需要官方来源 + 后端 + AI 接口，三样都没有 |

**所以本期能做的只有一件：把想法沉淀成设计**（写进 1.6 与 5.4）。设计先行不是拖延 —— 它让将来动手时不必把这些重新想一遍。

### v1.1（2026-09-21，Day 4 自检后修订）

v1.0 写完后，按 Day 4 任务要求做了一次 AI 自检 —— **换的是"审查者"视角，不是"作者"视角**。结果是：**🔴 必须修改级 4 项，全部已改；🟡 建议修改级 6 项，一并处理。**

#### 🔴 必须修改级（会导致 Day 7 之后做不下去）

| # | 问题 | 修订 |
|---|---|---|
| **M1** | 4.4 说「热度值不可跨平台比较」、A2 又说「按热度值降序排列」—— **自相矛盾**。既然数值不可比，就不能拿它排序 | 把「排序依据」与「客观热度结论」分开：明确热度值是「**排序权重**」，不是「跨平台客观热度」；A2 改为可逐条验证的"上一条 ≥ 下一条" |
| **M2** | E3 写的是"有明确说明**或约定**" —— "或约定"**没法判定**，违反"验收标准只能判通过/不通过"的规矩 | 删掉"或约定"，改为"页面上必须有一处可见文字" |
| **M3** | 4.2 把「排名」列为数据的必填字段，4.3 又要求筛选后重新编号 —— **数据里存了排名，筛完听谁的？** | 区分「数据字段（3 个）」与「显示字段（4 个）」，明确排名由前端实时计算、不存进数据 |
| **M4** | B2 用「微博」验证"只匹配标题"，但**没有任何条款保证标题里不含"微博"** —— 这条标准可能永远无法通过 | 在 4.4 增加「示例数据的准备要求」，明确规定标题不得含平台名、热度值互不相同 |

#### 🟡 建议修改级（影响体验/埋坑，但不致命）

| # | 问题 | 修订 |
|---|---|---|
| **Y1** | 4.1 结构图第四个区块标成 `③'`，与正文"4 个区块"对不上 | 改为 `④` |
| **Y2** | 2.2 场景写"地铁上打开"，但 5.3 明确本期不做移动端 —— 两处打架 | 改为"刚到教室 / 工位" |
| **Y3** | C1 只说"搜索引擎"，没指定哪一家，没法验收 | 固定为百度，并写明理由 |
| **Y4** | 没考虑标题含 `&`、`#`、空格时的 URL 编码 —— **会导致跳转链接断裂**，典型的低级 bug | 4.3 增加 URL 编码要求 |
| **Y5** | 没规定"输入框只有空格"怎么办 | 增加交互 6：视为空，恢复全量，不触发空状态 |
| **Y6** | 待定问题第 3 条想把热度格式化成「万」，但**格式化后按字符串排序会出错**（`1.9万` 竟排在 `874498` 前面） | 该条下补充提醒：格式化只影响显示，排序必须用原始数值 |

#### 还有两处 🟢 可选优化（未改，留作后续）

- 本文档目前把"用户故事"省掉了。若有余力，可把 B1（关键词筛选）写成用户故事。**这正好是 Day 4 的余力加练题。**
- "热度值排序"在数据换成真实数据源之后需要重新定义规则 —— 已记在第 6 节待定问题第 6 条。

### v1.0（2026-09-21，Day 4 初稿）

按 `research.md`（Day 3）的结论撰写，含 5 项已拍板决策：单页面 / 只匹配标题 / 4 个显示字段 / 实时筛选 / 点击跳搜索引擎。
