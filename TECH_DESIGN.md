# 今日热搜网站 · 技术设计文档（TECH_DESIGN.md）

> **文档版本**：v1.0
> **撰写日期**：2026-09-22（Day 5）
> **适用项目**：今日热搜网站
> **上游文档**：`PRD.md`（Day 4 产品需求文档）、`research.md`（Day 3 需求研究）
> **文档性质**：本文档规定「**用什么做、东西怎么摆、数据怎么流动**」——**不含代码**

---

## 1. 这份文档管什么

### 1.1 一句话总结

> **用最基础的三种网页技术（HTML、CSS、JavaScript）做一个不需要服务器、不需要数据库的静态网站**：30 条数据放在一个文件里，浏览器打开就读、就排、就显示，用户输入关键词就地筛选。

### 1.2 与 PRD 的分工

两份文档长得像，但回答的问题完全不同：

| | `PRD.md`（Day 4 已完成） | `TECH_DESIGN.md`（今天） |
|---|---|---|
| 回答的问题 | **做成什么样才算完成** | **用什么做、东西怎么摆、数据怎么流动** |
| 面向 | 验收（做到没有） | 施工（怎么动手） |
| 语气 | 要求、标准 | 方案、理由 |
| 举例 | 「输入关键词后列表立即只剩匹配条目」 | 「这个筛选动作由 `app.js` 完成，数据从 `data.js` 来」 |

**一句话**：PRD 是效果图 + 验收单，技术设计是**施工图和材料清单**。

### 1.3 本文档不做什么

- **不写代码**（Day 5 任务边界：「今日不做：写代码」）
- **不改 PRD 的结论**（本期功能范围仍以 `PRD.md` 为准，Day 5 任务边界：「今日不做：回头大改 PRD」）
- **不做界面设计**（配色、字号、间距属于 Day 7 开发时的事）

---

## 2. 技术路线

### 2.1 选定路线

> **纯静态三件套 ＝ HTML + CSS + 原生 JavaScript**
>
> **不用框架 · 不用构建工具 · 不用服务器 · 不用数据库**

「三件套」指的是网页最基础的三样技术：

| 技术 | 一句话 | 在这个项目里的活 |
|---|---|---|
| **HTML** | 骨架（有哪些东西） | 标题、输入框、列表容器 |
| **CSS** | 长相（东西什么样） | 颜色、间距、字号 |
| **JavaScript** | 行为（点了会怎样） | 排序、筛选、点击跳转 |

### 2.2 为什么选它（三条理由）

**理由一：项目复杂度正好对得上。**

这个项目的本质是「把 30 条数据排好序显示出来 + 一个筛选框」，原生写法大约 150 行就够。而框架解决的是「大型项目代码难维护」——**这个项目遇不到那个问题**。用框架属于杀鸡用牛刀，而且牛刀还得先磨三天。

**理由二：零环境依赖，出错概率最低。**

不用 npm、不用编译、不用起服务器、不用命令行。**双击就能打开，改一行双击就能看到效果。**

对零基础来说，最浪费时间的不是"不会写"，而是**"环境问题伪装成代码问题"**——明明代码没错，页面就是白的，然后花半天到处找原因。这条路线把这类问题压到最少。

**理由三：学到的是底层，将来换框架不亏。**

原生 DOM 操作、事件处理、数组方法，是所有前端框架的**共同地基**。反过来，先学框架容易出现「能跑起来，但不知道为什么」。将来真要升级到 Vue，那是**平移**，不是重学。

### 2.3 被否掉的两条路（记录理由，防止以后动摇）

| | **选定：纯静态三件套** | 否掉：Vue 3（CDN 引入） | 否掉：React + Vite（工程化） |
|---|---|---|---|
| **做法** | 三个文件，浏览器直接跑 | 引一个网上的框架库，用它的写法 | 要装 npm、打包、构建的完整工程 |
| **要装东西吗** | 什么都不用装 | 不用装，但**依赖网络**（引不到库就白屏） | 要装一堆包 |
| **环境搭建耗时** | 0 | 约半天（要理解框架概念） | **1～2 天**（版本、依赖、构建） |
| **本项目代码量** | 约 150 行 | 约 120 行 | 约 200 行 + 一堆配置文件 |
| **出错时的排查难度** | 低（报错直白） | 中（多一层框架自己的错） | 高（可能错在配置而不是代码） |
| **否掉 / 选中的原因** | **复杂度匹配、环境最干净、学的是地基** | 不是不好，是**现在不需要**；将来想升级随时可以 | 解决的问题（大型项目维护）**本项目不存在** |

### 2.4 技术栈一览

> **先说"技术栈"这个词是什么意思**：它指**做这个东西用到的所有技术的集合**。就像装修的**材料清单**——水泥、瓷砖、电线，每样都有它的位置。行业里说「我的技术栈是 XXX」，意思就是「我用这些东西干活」。

本项目的技术栈，全部列在这里：

| 技术 / 工具 | 它是什么 | 在这个项目里干什么 | 为什么用它 |
|---|---|---|---|
| **HTML** | 网页的骨架语言 | 定义页面上有哪些元素 | 浏览器只认它，没有替代品 |
| **CSS** | 网页的样式语言 | 决定颜色、间距、字号 | 同上 |
| **JavaScript** | 网页的编程语言 | 排序、筛选、点击行为 | 浏览器里能跑的就它 |
| **JSON** | 一种**数据格式**（不是编程语言） | 描述"一条热点"长什么样 | 通用格式，和将来服务器接口返回的格式一致 |
| **浏览器** | 运行环境 | 整个项目跑在这里 | 电脑上已经有了，不用装不用钱 |
| **Git / GitHub** | 版本管理 + 代码托管 | 存代码、记录每天改动 | Day 2 已学 |
| **GitHub Pages** | 免费的静态网站托管 | 把网站发布到互联网上 | 免费、学生作品集最常用 |

**注意最后两行**：它们是**工具**，不是写网站要用的技术。所以本项目真正要学的"技术"只有前三样 + 一种数据格式。

### 2.5 技术约束（红线）

以下几条是本次技术设计的**硬约束**，写下来是为了防止开发过程中手痒：

| # | 约束 | 为什么 |
|---|---|---|
| 1 | **不引入任何前端框架**（React / Vue / jQuery 等） | 见 2.2 理由一 |
| 2 | **不引入构建工具**（npm / webpack / Vite 等） | 见 2.2 理由二 |
| 3 | **不使用后端，不建数据库** | 本期数据是手写的，用不上（见 `PRD.md` §5.2） |
| 4 | **不引入任何联网加载的外部库**（CDN） | 保证**断网也能打开**，也让"打不开"永远不可能是网络问题 |
| 5 | **不使用需要编译的语言**（TypeScript 等） | 多一层工具就多一层坑 |
| 6 | **不抓取任何网站的数据** | 法律红线，见 `research.md` §7 与 `PRD.md` §5.2 |

---

## 3. 目录结构

### 3.1 本期结构

```
Vibe-coding/                      ← 学习过程仓库（当前所在）
├── AGENTS.md                     项目规则
├── research.md                   Day 3 需求研究
├── PRD.md                        Day 4 产品需求文档
├── TECH_DESIGN.md                ← 本文档（Day 5）
├── docs/
├── Picture/                      每日作业截图归档
└── Hot-search/                   ← 今日热搜网站（本期在这里开发）
    ├── index.html                页面骨架
    ├── style.css                 样式
    ├── app.js                    逻辑（排序 / 筛选 / 点击）
    └── data.js                   30 条示例数据
```

**本期只会往 `Hot-search/` 里加文件，不动其它目录。**

### 3.2 为什么单独放一个文件夹

因为**将来部署网站时，被"公开"的是整个文件夹**。

GitHub Pages 的工作方式是：指定一个目录，把它整个变成网站。如果 `index.html` 直接放在仓库最外层，那这个仓库里的 `research.md`、`PRD.md`、`AGENTS.md`、`Picture/` 里的所有截图，**都会变成"网站的一部分"被公开访问**。

单独放一个文件夹，将来只需指定这个文件夹，其它学习档案就不会被卷进去。

### 3.3 命名约定

| 对象 | 约定 | 说明 |
|---|---|---|
| 项目文件夹 | `Hot-search` | 本人指定。**注意**：路径区分大小写，GitHub Pages 上的地址会是 `.../Hot-search/`，以后在任何地方引用它都要**保持大小写完全一致**，否则会出现"明明文件在却打不开" |
| 文件名 | 全小写英文 + 点号分隔 | `index.html`、`style.css`、`app.js`、`data.js` |
| 不用中文文件名 | — | 中文路径在部署、压缩、跨系统时容易出乱码 |

### 3.4 将来的归属

`AGENTS.md` 附 5 规定：三个作品**各自独立建仓库**（目前尚未创建，计划 Day 7 开始做 MVP 时再建）。

到那时，`Hot-search/` 这个文件夹**整体搬过去**，内部结构不变。对应的设计文档（`research.md`、`PRD.md`、`TECH_DESIGN.md`）也会一起复制过去——因为它们是这个作品的设计依据，理应跟着作品走。本仓库保留一份作为学习档案。

---

## 4. 每个文件干什么

| 文件 | 职责 | 大概行数 | 依赖谁 |
|---|---|---|---|
| `index.html` | 页面**骨架**：标题、说明文字、关键词输入框、列表容器、空状态提示的位置 | 约 40 行 | 引入 `style.css`、`data.js`、`app.js` |
| `style.css` | 页面**长相**：配色、间距、字号、列表排版 | 约 80 行 | 无 |
| `data.js` | **数据**：30 条示例热点 | 约 40 行 | 无（被 `index.html` 引入） |
| `app.js` | **逻辑**：读数据 → 排序 → 算排名 → 渲染列表 → 响应输入框筛选 → 处理点击 | 约 150 行 | 依赖 `data.js` 里的数据 |

**加载顺序很重要**（写进 `index.html` 的先后顺序）：

```
data.js  →  app.js
（先有数据，再有处理数据的逻辑）
```

**一个关键设计：取数只在一个地方**

`app.js` 里会有一个专门负责"拿到 30 条数据"的地方。**所有用到数据的地方都从这里取，不直接去碰 `data.js`。**

为什么这么做？因为将来数据源会换两次（换真实数据、换按天存储），换的时候**只改这一个地方**，其它逻辑一行都不用动。这是零成本的设计，但省掉的是后面的大返工。

---

## 5. 数据流图（★ 核心）

### 5.1 本期数据流图

```flow
数据流图 · 本期（示例数据阶段）
════════════════════════════════════════════════════════════════

  [1] 数据源 · data.js
      30 条手写数据（标题 / 来源平台 / 热度值）
              │
              │  打开网页时，随页面一起加载（不需要联网）
              ▼
  [2] 读取与排序 · app.js
      读进内存  →  按热度值从高到低排序  →  算出排名 1~30
              │
              ▼
  [3] 渲染列表 · index.html + style.css
      显示 30 条，每条 4 项：排名 · 标题 · 来源平台 · 热度值
              │
              │  用户在输入框里敲关键词（边敲边变，不用按回车）
              ▼
  [4] 筛选 · app.js
      只拿关键词比对【标题】这一个字段
              │
              ├─ 有匹配  →  回到 [3] 重新显示剩下的条目（排名重编 01~N）
              └─ 无匹配  →  显示空状态提示「没有找到相关内容」

════════════════════════════════════════════════════════════════

  另一条分支：点击条目（出去之后不回来）

  [5] 用户点击任意一条
              │
              ▼
      新窗口打开搜索引擎结果页（搜索词 = 该条的完整标题）
      ★ 网站本身不跳转、不记录、不改变任何数据
```

### 5.2 逐步说明

| 步 | 谁在做 | 数据发生了什么 | 对应 PRD |
|---|---|---|---|
| [1] | 浏览器 | 打开网页时，把 `data.js` 里的 30 条读进来 | §4.4 数据说明 |
| [2] | `app.js` | 按热度值从高到低排；排名不是数据里存的，是**算出来的** | §4.2 字段定义 |
| [3] | `index.html` + `style.css` | 生成 30 条列表显示出来 | §4.1 页面结构 |
| [4] | `app.js` | 关键词只与**标题**比对；结果为空时换成提示文字 | §4.3 交互定义 |
| [5] | 浏览器 + 外部网站 | 新窗口打开搜索引擎，**当前页面不动** | §5.1「砍掉一」 |

### 5.3 一个必须说清的事实

**本期网站是"只读"的。**

```
用户的所有操作 → 都不会产生新数据
              → 都不会被保存下来
              → 关掉页面，下次打开还是原来那 30 条
```

没有账号、没有收藏、没有历史记录、没有"上次输过的关键词"。这不是偷懒，是 `PRD.md` §5.2 里明确砍掉的（那些都需要数据库）。

### 5.4 未来数据流图（本期不实现，仅作预留）

留着这张图，是为了说明**将来加后端时，前端不会被推倒重做**。

```flow
数据流图 · 将来（接入真实数据之后，本期不实现）
════════════════════════════════════════════════════════════════

  [1] 数据源 · 后端定时抓取
      跑在服务器上，每天固定时间去取各平台热点
      （人不用打开网页，它自己会跑）
              │
              ▼
  [2] 存储 · 数据库
      把每天的数据存一份
      →  攒够 7 天，就能画「一周热度趋势曲线」
              │
              ▼
  [3] 接口 · 后端提供数据
      前端来问：给我今天的数据 / 给我这个词最近 7 天的数据
              │
              ▼
  [4] 前端 · 结构基本不变
      拿到的数据字段格式和现在一样
      →  第 [2]~[5] 步那套「排序、渲染、筛选」的逻辑几乎不用改

════════════════════════════════════════════════════════════════

  一句话：将来加后端，前端不是「推倒重做」，而是「换个地方拿数据」
```

### 5.5 一句话回答「数据从哪来、到哪去」

> **数据来自项目里的一份手写数据文件（`data.js`），随网页一起被浏览器加载；`app.js` 把它按热度值排好序、算出排名、渲染成列表；用户输入的关键词也在 `app.js` 里就地完成筛选；点击条目则跳去搜索引擎。**
>
> **全程没有服务器、没有数据库，网站只读不写 —— 用户的操作不会产生、也不会留下任何新数据。**

---

## 6. 数据说明

### 6.1 数据放在哪、什么格式

放在 `Hot-search/data.js`，**格式与 JSON 一致**（字段名、嵌套方式都一样），只是**外面包了一层 JavaScript 的写法**，让它能随页面一起加载。

**格式示意（这不是代码实现，真正的数据文件 Day 7 才写）：**

| 字段 | 类型 | 说明 | 示例 |
|---|---|---|---|
| `title` | 文本 | 热点标题 | 某品牌发布新款折叠屏手机 |
| `platform` | 文本 | 来源平台 | 微博 |
| `heat` | 数字 | 热度值（**仅作排序权重**，含义见 `PRD.md` §4.4） | `874498` |

**注意**：数据里**不存排名**。排名是给人看的顺序，由 `app.js` 每次算出来（`PRD.md` §4.2 已规定）。

30 条数据必须满足 `PRD.md` §4.4 的三条准备要求（正好 30 条、标题里不得出现平台名、热度值互不相同）。

### 6.2 为什么用 `data.js` 而不是 `data.json`

这是本次技术设计里**唯一一个"为了跑得通而做的妥协"**，必须写清楚。

| | `data.json` | `data.js`（选定） |
|---|---|---|
| 双击打开网页能看到效果吗 | **不能**，页面会空白 | **能** |
| 原因 | 浏览器有安全规定：**不许网页偷偷读本地文件**。双击打开时网页地址是 `file://`，读旁边文件会被拦住 | 数据是随页面一起加载的，不走"读文件"这条路，浏览器不拦 |
| 代价 | 每次看效果前要先起一个"本地服务器" | **将来换真实数据时，要多改约 3 行代码** |

**决定理由**：这个阶段最要紧的是**反馈快** —— 改一行、双击、立刻看到效果，比"改一行、起服务、开浏览器、发现忘了保存"强太多。而那 3 行代码的代价，等到真需要时再付。

**关键点**：两者的**数据格式完全一样**，所以将来切换时，**数据结构不用动，只改"怎么拿到数据"**——正好落在 4 节说的那个"取数只在一个地方"上。

### 6.3 与 PRD 的对应关系

| PRD 里的要求 | 本文档的落实 |
|---|---|
| §4.2 条目显示 4 项（排名 / 标题 / 来源平台 / 热度值） | 数据里存 3 项，排名由 `app.js` 算出 |
| §4.3 筛选只匹配标题、边打边筛、排名重编 | 全部由 `app.js` 的筛选逻辑承担 |
| §4.4 排序按热度值递减、不宣称全网热度 | 排序在 `app.js`；页面上会标明是示例数据 |
| §7 验收标准（17 条） | 开发完成后逐条打勾，见 `PRD.md` §9 验收记录表 |

---

## 7. 将来加功能时，会动到哪些文件

这一节回答一个很实际的问题：**"如果以后想加某某功能，要动多少东西？"**

| 想加的功能 | 新增什么 | 要改什么 | 还需要哪些新能力 |
|---|---|---|---|
| 更多平台的数据 | `data.js` 加条目 | 无 | 无（数据从哪来是另一件事） |
| 平台筛选 Tab | 无 | `index.html`（加 Tab）、`style.css`、`app.js`（多一个筛选条件） | 无 |
| 多关键词 / 屏蔽词 | 无 | `app.js` 的匹配逻辑 | 无 |
| 记住我输过的关键词 | 无 | `app.js` | **浏览器本地存储**（不用后端） |
| **关键词一周热度趋势曲线** | 画图所需的文件 + 画图逻辑 | `app.js`（筛选逻辑要从"筛条目标题"改成"按天取数"）、**`data.js` 的数据结构要改成按天存放** | **① 连续 7 天的数据快照 → ② 后端定时抓取 → ③ 数据库 → ④ 画图能力** |
| AI 辅助解读官方文件 | 一个后端接口文件 | `app.js`（改成去调接口） | 后端 + AI 接口密钥（**绝不能写在前端**） |
| 推送通知 | 一整套后端服务 | 前端不参与 | 后端 + 定时任务 + 推送渠道 + 用户订阅关系 |

### 7.1 从这张表能看出两件事

**第一件：大多数"加功能"都只动 `app.js`。**

因为页面骨架（`index.html`）和长相（`style.css`）是**稳定的**，变的永远是"怎么处理数据"。这也反过来验证了 2.2 的选择是对的——逻辑集中在一个文件里，改起来范围清楚。

**第二件：热度趋势曲线是这里唯一一个「会倒逼底层重做」的功能。**

其它功能都是"往上加东西"，它不一样——它要求**数据从一开始就按天存着**。而本期数据是一天的、扁平的，攒不出趋势。所以它的真正门槛不是"画线"（画线很简单），而是：

```
没有连续 7 天的数据  →  画不出趋势
要拿到连续 7 天数据  →  必须有人每天定时去取、并且存下来
                      →  这就必须有后端 + 数据库
```

**这正是它被放进 `PRD.md` §5.4「未来想做」而不是本期功能的原因。** 它卡的不是我们不会画图，是"数据还没有来处"。

### 7.2 本期为它预留了什么

本期不做它，但**做了一个成本为零、将来能省大返工的预留**：

| 预留动作 | 将来省掉什么 |
|---|---|
| **取数只写在一个地方**（4 节） | 数据源从"文件"换成"服务器接口"时，只改这一处，不用全文件找 |
| **数据字段名与真实数据保持一致**（6.1） | 换真实数据时，渲染逻辑不用改 |
| **数据里不存排名，排名算出来**（6.1） | 换数据源后，排名逻辑天然还是对的 |

一句话：**本期不做趋势曲线，但把"将来能接上它"的位置留好了。**

---

## 8. 待定问题

| # | 问题 | 什么时候定 |
|---|---|---|
| 1 | 页面宽度、配色、字号 | Day 7 开发时 |
| 2 | 热度值要不要显示成「万」（如 1.9万） | Day 7 开发时。**注意**：只影响显示，排序必须用原始数值（`PRD.md` §6） |
| 3 | 网站名「今日热搜」是否要改 | 部署上线前 |
| 4 | 具体用哪个搜索引擎（百度 / 必应 / ……） | Day 7 开发时 |
| 5 | 是否要加一个"回到顶部"按钮（列表 30 条时可能有用） | Day 7 开发时看实际效果 |
| 6 | 真实数据源怎么接（`data.json` + 接口） | 28 天课程结束后 |
| 7 | 将来的后端用什么写 | 做 7 节里那三个功能之前 |
| 8 | 趋势曲线的数据怎么按天存 | 做「热度趋势曲线」之前 |

---

## 9. 下一步

| 时间 | 做什么 |
|---|---|
| **Day 6** | 按课程安排推进 |
| **Day 7 起** | 在 `Hot-search/` 里写四个文件（先骨架、再数据、再逻辑、最后样式） |
| 开发完成后 | 用 `PRD.md` §7 的 17 条验收标准逐条打勾 |
| Day 27 前后 | 部署到 GitHub Pages |
| 课程结束后 | 接真实数据 → 后端 + 数据库 → 再谈趋势曲线和 AI 解读 |

---

## 附录：本文档的设计依据

| 本文档的结论 | 依据 |
|---|---|
| 技术路线 = 纯静态三件套 | 本人 Day 5 决策（三套方案对比后选定） |
| 数据用 `data.js` 不用 `data.json` | 本人 Day 5 决策（权衡"双击能开"与"改动最小"） |
| 项目放 `Hot-search/` 子文件夹 | 本人 Day 5 决策（避免部署时公开整个仓库） |
| 取数只写在一个地方 | 技术设计时新增，为将来换数据源预留 |
| 本期不做后端 / 数据库 | `PRD.md` §5.2、`research.md` §7 |
| 热度趋势曲线归入未来 | `PRD.md` §5.4 第 2 项（本人 Day 4 提出） |
